# ML_Projects/requirements.txt

# Core Streamlit library for the web app
streamlit

# Library to load the .pkl model file
joblib

# For array manipulation
numpy

# Required for image loading and processing (PIL/Pillow)
Pillow

# Scikit-learn must be included if the model was trained with it
scikit-learn
